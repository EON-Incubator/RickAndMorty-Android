query CharactersQuery($name:String!){
    characters(filter:{name:$name}) {
        results {
            id
            name
            image
            species
            status
            gender
        }
    }
}