query CharactersQuery($name:String!, $page:Int!) {
    characters(filter:{name:$name},page: $page) {
        ...pageInfo
        results {
            id
            name
            image
            species
            status
            gender
        }
    }
}