query CharactersQuery($filter:FilterCharacter!, $page:Int!) {
    characters(filter:$filter,page: $page) {
        ...pageInfo
        results {
            id
            name
            image
            species
            status
            gender
        }
    }
}