query GetEpisodeById($episodeId: ID!) {
    episode(id: $episodeId) {
        id
        name
        episode
        air_date
        characters {
            id
            name
            image
            gender
            species
            status
        }
    }
}