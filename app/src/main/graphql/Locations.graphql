query AllLocations($filter:FilterLocation!) {
    locations(filter:$filter) {
        results {
            id
            name
            type
            dimension
            residents {
                image
            }
        }
    }
}