query SpecificCharacter($characterId: ID!) {
    character(id: $characterId) {
        id
        episode {
            id
            name
            episode
            characters {
                image
            }
            air_date
        }
        image
        name
        species
        status
        gender
        location {
            dimension
            created
        }
    }
}
